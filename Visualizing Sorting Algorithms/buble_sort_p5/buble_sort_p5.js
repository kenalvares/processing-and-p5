let lines = [];
let count = 0, comparisons = 0;

function setup() {
  canvas(800, 500);
  for(let i = 0; i < lines.length; i++ ) {
    lines[i] = random(height);
  } 
}


function draw() {
  background(0);

}
